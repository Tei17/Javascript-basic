// Làm bài tập tại đây

function checkCanAccessible(age) {
    var isAccessible;
    
    if(age >= 16){
        isAccessible = true;
    } else {
        isAccessible = false;
    }

    return isAccessible;
}